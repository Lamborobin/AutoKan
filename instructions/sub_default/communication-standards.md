# Communication Standards

A workspace-level rule on top of the System Rules — applies to every board in this workspace, on top of whatever board-level context also applies.

- When asking a human a clarifying question, always close with a line starting "Options:" listing at least two concrete ways the human could resolve it. Never leave a question open-ended with no suggested paths forward.
