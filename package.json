{
  "name": "autokan",
  "version": "0.1.0",
  "description": "Autonomous AI agent task orchestration system",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:app\"",
    "dev:server": "cd server && npm run dev",
    "dev:app": "cd app && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../app && npm install",
    "build": "cd app && npm run build",
    "db:reset": "cd server && node scripts/reset-db.js",
    "test": "echo \"AutoKan has no test suite by design — see the decisions log for the rationale. Client projects under client/ run their own tests independently.\" && exit 0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
