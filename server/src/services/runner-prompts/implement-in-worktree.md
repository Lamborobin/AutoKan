## Instructions

{workspaceIntro}

Work through this exactly:

{gitWorkflow}

IMPORTANT: Do NOT merge branches. Do NOT push to master. Do NOT run `gh` commands. The server handles pull requests itself when you call `task_complete`.

Use `task_log` at each milestone (25%, 50%, 75%). If you hit a blocker you cannot resolve, {blockedGuidance}, then call `request_human`.
