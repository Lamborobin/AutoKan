PORT=3001
FRONTEND_URL=http://localhost:5173
DB_PATH=./data/autokan.db
ANTHROPIC_API_KEY=sk-ant-api03-Qy...

# Google OAuth — get from console.cloud.google.com → APIs & Services → Credentials
GOOGLE_CLIENT_ID=161618...-....apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GO...

# JWT signing secret — change this in production
JWT_SECRET=a...

# Email — Gmail SMTP for invite and @mention notifications
EMAIL_FROM=...@....se
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USER=...@....se
EMAIL_SMTP_PASS=....

# GitHub — for automatic PR creation after dev agent completes
GITHUB_TOKEN=ghp_c2...